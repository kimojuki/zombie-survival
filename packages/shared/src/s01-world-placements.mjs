/**
 * Seed décor S01.
 *
 * La forêt de départ repart volontairement sans POI/camp seed afin de garder la
 * plage + sentier propres. Les arbres, rochers, palmiers et panneaux plage sont
 * seedés par leurs modules dédiés.
 */
export function computeS01DecorPlacements() {
  return [];
}
