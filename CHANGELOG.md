# Changelog

All notable changes are tracked here. This project follows a staging flow:
`feature/* -> dev -> master`.

## Unreleased

- Restructured the repository toward `apps/client`, `apps/server`, `packages/shared`, `infra`, `docs/adr` and `tools/visual-tests`.
- Added studio contribution, security, PR and AI workflow protocols.
- Introduced Vite, ESLint, Prettier, smoke tests and CI configuration.
